# Gap-Silent Job Application Tailoring Agent

## Who has this problem
REPLACE_ME. Be specific: a frontend-leaning full-stack developer applying to
international remote roles, with a defined set of gaps (no Docker depth, no
WordPress, no competitive programming), who needs every application tailored
to the JD without ever naming those gaps.

## What bottleneck makes it worth solving
REPLACE_ME. For every JD, deciding which project to lead with and rewriting
bullets to mirror the JD's language, while never accidentally surfacing a gap,
is slow and easy to get subtly wrong under time pressure.

## Does the agent solve it well
REPLACE_ME after running the eval — summarize the baseline vs. agent
comparison from eval/results.csv in 2-3 sentences.

## Can another person reproduce the result
See REPRODUCE.md.

## What existed before this competition vs. what was added
- Existing: personal resume/project data, past manually-written application
  emails, the general practice of tailoring applications.
- Added during the hackathon: the structured experience bank, the baseline
  script, the draft/verify/revise agent pipeline, the evaluation harness,
  and the trajectory logging.

## Improvement Changelog

| Stage | What you tried and why | Evidence | Decision / learning |
|---|---|---|---|
| Baseline | One plain prompt, raw resume text, no structure | REPLACE_ME | Established the starting point |
| Iteration 1 | Added structured experience bank as context | REPLACE_ME | REPLACE_ME |
| Iteration 2 | Added verification pass to catch unsupported claims / named gaps | REPLACE_ME | REPLACE_ME |
| Iteration 3 | REPLACE_ME | REPLACE_ME | REPLACE_ME |
| Final | Combined the changes that worked | REPLACE_ME | Identified the main contribution |

## Main failure mode
REPLACE_ME — the honest one from testing, e.g. "the plain-prompt baseline
tends to soften a JD's heavy Docker/WordPress emphasis into an 'areas for
growth' framing, which quietly names the gap it was supposed to stay silent
on."

## Hot take
REPLACE_ME — one sentence turning the failure mode above into a practical
lesson for building more reliable agents.
